#include<bits/stdc++.h>
using namespace std;

int32_t main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);

	vector<string> v{
		"geekforgeeks practice",
		"geeksforgeeks ide",
		"geeksforgeeks write"
	};

	for (const auto &it : v) {
		cout << it << endl;
	}


	return 0;
}